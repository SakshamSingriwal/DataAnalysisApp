# 🏗️ System Architecture\n\nThis document describes the overall architecture and design patterns of DataAnalysisApp.\n\n---\n\n## 📊 High-Level Architecture\n\n```\n┌─────────────────────────────────────────────────────────────────┐\n│                       Streamlit Frontend                         │\n│                    (Web-based UI/UX)                            │\n└──────────────────────────┬──────────────────────────────────────┘\n                           │\n                           ↓\n┌─────────────────────────────────────────────────────────────────┐\n│                     Main Application                             │\n│                      (app.py)                                    │\n│  ┌─────────────────────────────────────────────────────────┐    │\n│  │ Session State Management & Data Flow Orchestration       │    │\n│  │ - User Input Handling                                    │    │\n│  │ - Page Navigation                                        │    │\n│  │ - Data Pipeline Coordination                             │    │\n│  └─────────────────────────────────────────────────────────┘    │\n└──────────────────────────┬──────────────────────────────────────┘\n                           │\n          ┌────────────────┼────────────────┬────────────────────┐\n          ↓                ↓                ↓                    ↓\n    ┌─────────────┐ ┌──────────────┐ ┌────────────┐ ┌──────────────┐\n    │  Core I/O   │ │ Data Process │ │  Analysis  │ │   Modeling   │\n    │             │ │              │ │            │ │              │\n    │ File        │ │ Preprocess   │ │ Stats      │ │ ML Models    │\n    │ Handler     │ │              │ │ Tests      │ │              │\n    │             │ │ Merge        │ │            │ │ Deep Learning│\n    │ Utils       │ │ Helpers      │ │ Correlation│ │              │\n    └─────────────┘ └──────────────┘ └────────────┘ └──────────────┘\n          │                ↓                ↓                    ↓\n          │         ┌──────────────┐ ┌────────────┐ ┌──────────────┐\n          │         │   Pandas     │ │  SciPy     │ │  TensorFlow  │\n          │         │   NumPy      │ │  StatsModl │ │  Scikit-learn│\n          │         │              │ │            │ │  XGBoost/    │\n          └─────────┤              │ │            │ │  LightGBM/   │\n                    │              │ │            │ │  CatBoost    │\n                    └──────────────┘ └────────────┘ └──────────────┘\n                           ↓                ↓                    ↓\n                    ┌──────────────────────────────────────────────┐\n                    │     External Libraries & Frameworks          │\n                    │  - Plotly (Visualization)                    │\n                    │  - SHAP (Model Interpretation)               │\n                    │  - Imbalanced-learn (Class Imbalance)        │\n                    └──────────────────────────────────────────────┘\n```\n\n---\n\n## 🔌 Module Dependencies\n\n### Dependency Graph\n\n```\napp.py (Main Orchestrator)\n├── file_handler.py\n│   └── (pandas, numpy, streamlit)\n├── utils.py\n│   └── (pandas, numpy, streamlit)\n├── preprocessing.py\n│   ├── (pandas, numpy, streamlit)\n│   └── sklearn.preprocessing, sklearn.impute\n├── ml_models.py\n│   ├── (pandas, numpy, streamlit)\n│   ├── sklearn.model_selection\n│   ├── sklearn.preprocessing\n│   ├── sklearn.(linear_model, tree, ensemble, cluster, svm, neighbors, naive_bayes)\n│   ├── xgboost, lightgbm, catboost\n│   ├── sklearn.metrics\n│   └── shap\n├── deep_learning.py\n│   ├── (pandas, numpy, streamlit)\n│   ├── sklearn.preprocessing, sklearn.model_selection, sklearn.metrics\n│   ├── tensorflow.keras\n│   └── numpy\n├── stats_tests.py\n│   ├── (pandas, numpy, streamlit)\n│   ├── scipy.stats\n│   ├── statsmodels.api\n│   └── statsmodels.stats.proportion\n└── merge_helpers.py\n    └── (pandas, numpy)\n```\n\n---\n\n## 💾 Data Flow Architecture\n\n### Pipeline Stages\n\n```\nINGESTION\n    ↓\nInput: User uploads file\n    ↓\nfile_handler.read_any_file()\n    └─→ Output: (DataFrame, metadata, error_code)\n\n\nEXPLORATION\n    ↓\nInput: Raw DataFrame\n    ↓\nutils.compute_quality()\n    └─→ Output: Quality metrics, missing values, duplicates\n\n\nCLEANING\n    ↓\nInput: Raw DataFrame\n    ↓\npreprocessing.run_preprocessing()\n    ├─→ Handle missing values\n    ├─→ Remove duplicates\n    ├─→ Detect & handle outliers\n    └─→ Output: Cleaned DataFrame (session state)\n\n\nTRANSFORMATION\n    ↓\nInput: Cleaned DataFrame\n    ↓\npreprocessing.run_preprocessing() [continued]\n    ├─→ Encode categorical features\n    ├─→ Scale numerical features\n    ├─→ Engineer new features\n    └─→ Output: Transformed DataFrame (session state)\n\n\nANALYSIS\n    ↓\n├─ stats_tests.run_hypothesis_testing()\n│  ├─→ Normality tests\n│  ├─→ Parametric tests (t-tests, ANOVA)\n│  ├─→ Non-parametric tests\n│  └─→ Correlation analysis\n│\n└─ Exploratory plots & statistics\n   └─→ Distributions, correlations, summaries\n\n\nMODELING\n    ↓\n├─ ml_models.run_ml_models() [Traditional ML]\n│  ├─→ Classification models\n│  ├─→ Regression models\n│  ├─→ Clustering algorithms\n│  └─→ Output: Trained models + metrics\n│\n└─ deep_learning.run_deep_learning() [Neural Networks]\n   ├─→ Build custom architectures\n   ├─→ Train with early stopping\n   └─→ Output: Trained model + history\n\n\nPREDICTION & EXPORT\n    ↓\n├─→ Generate predictions\n├─→ Download results (CSV/Excel)\n└─→ Save trained models\n```\n\n---\n\n## 🎨 Session State Architecture\n\n### Session State Structure\n\n```python\nst.session_state = {\n    # File & Data\n    'uploaded_file': File,              # Original file object\n    'raw_df': DataFrame,                # Raw uploaded data\n    'data_metadata': Dict,              # File metadata\n    \n    # Preprocessing State\n    'preprocessed_df': DataFrame,       # Cleaned/transformed data\n    'encoders': Dict[str, Encoder],    # Fitted encoders (reusable)\n    'scalers': Dict[str, Scaler],      # Fitted scalers (reusable)\n    'prep_log': List[str],              # Preprocessing history\n    \n    # Analysis Results\n    'eda_results': Dict,                # EDA computations\n    'stats_results': Dict,              # Statistical test results\n    'correlation_matrix': DataFrame,    # Correlation data\n    \n    # Model Training\n    'models': Dict[str, Model],         # Trained models\n    'model_metrics': Dict,              # Performance metrics\n    'predictions': DataFrame,           # Model predictions\n    'trained_scaler': Scaler,          # For new predictions\n    'feature_importance': Series,       # Feature rankings\n}\n```\n\n---\n\n## 🔐 Error Handling Strategy\n\n### Error Handling Layers\n\n```\nTry-Except Blocks\n    ↓\n├─ Specific Exceptions (ValidationError, FileNotFoundError)\n│  └─→ Log & Display informative error message\n│\n├─ Data Quality Checks\n│  ├─→ Check for empty DataFrames\n│  ├─→ Validate data types\n│  └─→ Check for required columns\n│\n├─ Algorithm-Specific Errors\n│  ├─→ Insufficient data samples\n│  ├─→ Singular matrices\n│  └─→ Convergence failures\n│\n└─ Graceful Degradation\n   ├─→ Skip problematic modules\n   ├─→ Provide alternative approaches\n   └─→ Log warnings & recommendations\n```\n\n---\n\n## 🚀 Performance Optimization Strategies\n\n### 1. Caching\n\n```python\n# Streamlit's built-in caching\n@st.cache_data(ttl=3600)\ndef load_data(file):\n    return pd.read_csv(file)\n\n# Session state caching\nif 'preprocessed_df' not in st.session_state:\n    st.session_state.preprocessed_df = preprocess(df)\n```\n\n### 2. Lazy Loading\n\n- Models only loaded when selected\n- TensorFlow imported conditionally\n- Feature modules imported on-demand\n\n### 3. Data Sampling\n\n```python\n# Sample large datasets for exploration\nif len(df) > 100000:\n    df_sample = df.sample(n=10000, random_state=42)\n    st.info(\"Large dataset detected. Working with 10K sample.\")\n```\n\n### 4. Efficient Operations\n\n- Use vectorized pandas operations\n- Leverage NumPy for numerical computations\n- Use GPU when available (TensorFlow)\n\n---\n\n## 🔄 Integration Points\n\n### Module Communication\n\n```\napp.py acts as the Central Hub\n\nInput Reception\n    ↓\n    ├─ file_handler.py processes file\n    ├─ Stores in st.session_state\n    │\n    └─ User selects analysis type\n       ├─ Preprocessing\n       │  └─ preprocessing.py\n       │     └─ Updates st.session_state\n       │\n       ├─ Statistical Analysis\n       │  └─ stats_tests.py\n       │     └─ Displays results\n       │\n       ├─ Machine Learning\n       │  └─ ml_models.py\n       │     └─ Trains & evaluates models\n       │\n       └─ Deep Learning\n          └─ deep_learning.py\n             └─ Builds neural networks\n\nFinal Output\n    └─ Export results via utils.py\n```\n\n---\n\n## 🛡️ Configuration Management\n\n### Centralized via config.py\n\n```python\n# Color scheme\nfrom config import COLORS\nst.markdown(f'<div style=\"color: {COLORS[\"primary\"]}\">', unsafe_allow_html=True)\n\n# ML Parameters\nfrom config import TRAIN_TEST_SPLIT, CV_FOLDS\ntrain_test_split(X, y, test_size=TRAIN_TEST_SPLIT)\n\n# File limits\nfrom config import SUPPORTED_EXTENSIONS, MAX_UPLOAD_SIZE_MB\nif file.size > MAX_UPLOAD_SIZE_MB * 1024 * 1024:\n    st.error(\"File too large\")\n```\n\n---\n\n## 📈 Scalability Considerations\n\n### Current Limitations\n\n- Single-machine processing\n- File upload limited to configured size\n- In-memory DataFrame storage\n\n### Future Improvements\n\n- Distributed processing (Dask, Spark)\n- Database backend integration\n- Batch processing pipeline\n- Model serving API (FastAPI)\n- Result caching (Redis)\n\n---\n\n## 🧪 Testing Architecture\n\n### Test Pyramid\n\n```\n           /\\\n          /  \\\n         /Unit\\\n        /Tests\\\n       /────────\\\n      /Integration\\\n     /   Tests    \\\n    /──────────────\\\n   /   E2E Tests   \\\n  /──────────────────\\\n```\n\n### Test Categories\n\n1. **Unit Tests** - Individual functions\n2. **Integration Tests** - Module interactions\n3. **E2E Tests** - Full user workflows\n\n---\n\n## 📚 Design Patterns Used\n\n### Patterns\n\n1. **Module Pattern** - Organized code into separate modules\n2. **Factory Pattern** - Model creation in ml_models.py\n3. **Singleton Pattern** - Streamlit session state\n4. **Pipeline Pattern** - Data preprocessing pipeline\n5. **Observer Pattern** - Streamlit callbacks\n\n---\n\n## 🔍 Monitoring & Logging\n\n### Logging Strategy\n\n```python\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n# Log different levels\nlogger.debug(\"Detailed debug info\")\nlogger.info(\"Important information\")\nlogger.warning(\"Warning messages\")\nlogger.error(\"Error occurred\")\nlogger.critical(\"Critical failure\")\n```\n\n### Metrics to Monitor\n\n- File upload success rate\n- Model training time\n- Prediction latency\n- Memory usage\n- Error rates\n\n---\n\n## 🌐 Deployment Architecture\n\n### Deployment Options\n\n```\n┌─────────────────────────────────────────┐\n│      Development (Local Machine)        │\n│  streamlit run app.py                   │\n└─────────────────────────────────────────┘\n                    ↓\n┌─────────────────────────────────────────┐\n│   Streamlit Cloud / Heroku              │\n│  - Auto-restart on code changes         │\n│  - Built-in authentication              │\n│  - Public/Private sharing               │\n└─────────────────────────────────────────┘\n                    ↓\n┌─────────────────────────────────────────┐\n│    Docker Container (Production)        │\n│  - Consistent environment               │\n│  - Easy scaling                         │\n│  - CI/CD integration                    │\n└─────────────────────────────────────────┘\n```\n\n---\n\n## 🔐 Security Architecture\n\n### Security Measures\n\n1. **Input Validation** - File type & size checks\n2. **Error Handling** - Don't expose stack traces\n3. **Dependency Management** - Regular updates\n4. **Environment Variables** - Sensitive config in .env\n5. **Session Isolation** - Per-user session state\n\n---\n\n**For more details, see DEVELOPMENT.md for setup and contribution guidelines.**\n"